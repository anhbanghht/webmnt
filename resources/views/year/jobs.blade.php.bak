@extends('layouts.default')

@section('content')
<section>
    <div class="section-body contain-lg">
        <!-- BEGIN INTRO -->
    		<div class="row">
    			<div class="col-lg-12">
    				<h1 class="text-primary">Nội dung công việc trong {{$year->year}}</h1>
    			</div><!--end .col -->
    		</div><!--end .row -->
    	<!-- END INTRO -->
    	<div class="row">
        	<div class="col-lg-12">
    			<div class="card">
    				<div class="card-body">
    				    <a href="{{route('job::add')}}" class="btn ink-reaction btn-primary"><span class="fa fa-plus"></span></a>
    					<div class="table-responsive">
    						<table class="table no-margin">
    							<thead>
    								<tr>
    									<th>ID</th>
    									<th width="400px">Tên công việc</th>
    									<th>Loại thực tập</th>
    									<th>Ngày bắt đầu</th>
    									<th>Ngày kết thúc</th>
    									<th width="400px">Mô tả</th>
    									<th>Quan trọng</th>
    									<th>Cả ngày</th>
    									<th width="400px">Ghi chú</th>
    									<th width="400px">Hành động</th>
    								</tr>
    							</thead>
    							<tbody>
    							    @if( $job->isEmpty() )
    							    <tr>
    									<td colspan="10">Không có bản ghi nào!</td>
    								</tr>
    							    @else
    							    @foreach( $job as $item )
    							    <tr>
    									<td>{{$item->id}}</td>
    									<td width="400px">{{$item->job_name}}</td>
    									<td>{{$item->intership_type->intertype_name	}}</td>
    									<td>{{ Date('Y/m/d',strtotime($item->startdate)) }}</td>
    									<td>{{ Date('Y/m/d',strtotime($item->enddate)) }}</td>
    									<td width="400px">{{$item->description}}</td>
    									<td>{{$item->impor}}</td>
    									<td>{{$item->alld}}</td>
    									<td width="400px">{{$item->note}}</td>
    									<td width="400px">
    									    <a href="{{route('job::view',['id'=>$item->id])}}" class="btn ink-reaction btn-primary" data-toggle="tooltip" data-placement="top" data-original-title="Nội dung công việc"><span class="glyphicon glyphicon-eye-open"></span></a>
    									    <a href="{{route('job::edit',['id'=>$item->id])}}" class="btn ink-reaction btn-primary"><span class="glyphicon glyphicon-pencil" data-toggle="tooltip" data-placement="top" data-original-title="Sửa"></span></a>
    									    <a href="{{route('job::delete',['id'=>$item->id])}}" class="btn ink-reaction btn-primary remove-btn"><span class="glyphicon glyphicon-remove" data-toggle="tooltip" data-placement="top" data-original-title="Xóa"></span></a>
    									</td>
    								</tr>
    							    @endforeach
    								@endif
    							</tbody>
    						</table>
    					</div><!--end .table-responsive -->
    				</div><!--end .card-body -->
    			</div><!--end .card -->
    		</div>
		</div>
		
    </div>
</section>
@stop
@section('addtional-js')
<script>
    (function($){
        var token = "{{csrf_token()}}";
        $('.remove-btn').click(function(e){
           e.preventDefault();
           var link = $(e.currentTarget).attr('href');
           $.ajax({
               url: link,
               data: '_token='+token,
               method: 'POST'
           }).done(function(data){
              window.location.reload(); 
           });
        });
    })(jQuery);
</script>
@stop